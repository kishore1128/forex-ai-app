"""Utility functions used by the UI for extra AI analysis checks."""

from typing import Optional
import pandas as pd


def ema_crossover(indicators: dict) -> Optional[str]:

    return None


def rsi_divergence(df: pd.DataFrame) -> Optional[str]:

    return None